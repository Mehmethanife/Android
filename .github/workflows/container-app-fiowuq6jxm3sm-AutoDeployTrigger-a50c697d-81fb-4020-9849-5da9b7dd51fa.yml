name: Trigger auto deployment for container-app-fiowuq6jxm3sm

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ Mehmethanife-patch-1 ]
    paths:
    - '**'
    - '.github/workflows/container-app-fiowuq6jxm3sm-AutoDeployTrigger-a50c697d-81fb-4020-9849-5da9b7dd51fa.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPFIOWUQ6JXM3SM_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPFIOWUQ6JXM3SM_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPFIOWUQ6JXM3SM_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: containerregistryfiowuq6jxm3sm.azurecr.io
          registryUsername: ${{ secrets.CONTAINERAPPFIOWUQ6JXM3SM_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPFIOWUQ6JXM3SM_REGISTRY_PASSWORD }}
          containerAppName: container-app-fiowuq6jxm3sm
          resourceGroup: androidb114
          imageToBuild: containerregistryfiowuq6jxm3sm.azurecr.io/container-app-fiowuq6jxm3sm:${{ github.sha }}
          dockerfilePath: Dockerfile


